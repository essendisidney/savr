# Analysis options placeholder — run `flutter create .` in apps/mobile to generate full platform folders.
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
